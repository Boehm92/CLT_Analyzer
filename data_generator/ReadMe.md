Workflow:

* Change local path in .env file
* Create local directory data/cad/, for example: CLT_ANALYZER/data/cad/
* Set start and end cycle parameter in __main__.py
* Build Docker image and run docker compose.